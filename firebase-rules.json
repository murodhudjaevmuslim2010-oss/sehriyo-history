{
  "rules": {
    "rooms": {
      ".read": true,
      ".write": true,
      ".indexOn": ["code"]
    },
    "users": {
        "$uid": {
            ".read": "$uid === auth.uid",
            ".write": "$uid === auth.uid"
        }
    }
  }
}
