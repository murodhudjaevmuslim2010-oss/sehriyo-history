{
  "rules": {
    "rooms": {
      ".read": true,
      ".write": true,
      ".indexOn": ["code"]
    },
    "users": {
        ".read": "auth != null",
        "$uid": {
            ".write": "auth != null && auth.uid == $uid"
        }
    }
  }
}
